pipeline{
    agent any
    environment{
        fname="Tarvez"
    }
    stages{
        stage('one'){
                steps{
                    scripts{
                        if (env.fname == "Tarvez"){
                            echo "Hello ${fname}"
                        }
                            else{
                            echo "You are in Else block"
                    }

                }

            }
            }
        }
}
