pipeline{
    agent any
    environment{
        cred=credentials("test")
    }
    stages{
        stage('one'){
            steps{
                sh'''
                    echo "Hello ${test_USR}"
                    echo "Hello ${test_PWS}"
                '''
            }
        }
    }
}