pipeline{
    agent any
    environment{
        cred=credentials("test")
    }
    stages{
        stage('one'){
            steps{
                sh'''
                    pwd
                    echo "Hello ${test_USR}"
                    echo "Hello ${test_PWS}"
                    ls
                '''
            }
        }
    }
}