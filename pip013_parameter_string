pipeline{
    agent any
    parameters{
        string(name: 'fname', defaultValue: 'Tarvez', description: '')
    }
    stages{
        stage('one'){
            steps{
                
                    echo "Hello ${params.fname}"
                

            }
        }
    }
}